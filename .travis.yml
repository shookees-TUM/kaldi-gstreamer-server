sudo: required

language: python

services:
  - docker

before_install:
 - docker pull shookees/kaldi-gstreamer-server

script:
 - docker run -v `pwd`:/opt/kaldi-gstreamer-server-test shookees/kaldi-gstreamer-server -c "cd /opt/kaldi-gstreamer-server-test && python -m unittest discover kaldigstserver *_test.py"